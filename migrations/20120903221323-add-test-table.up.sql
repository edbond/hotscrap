DROP TABLE IF EXISTS notebooks;
CREATE TABLE notebooks (
  name varchar(100),
  proc varchar(100),
  "proc-speed" numeric,
  screen varchar(30),
  size varchar(30),
  price numeric,
  score numeric,
  grade numeric
   );
  
